{
  "name": "@moq-web/media-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end MOQT media conformance tests (WebCodecs) against a live relay",
  "type": "module",
  "scripts": {
    "test": "if [ \"$RUN_E2E\" = \"1\" ]; then vitest run --config vitest.browser.config.ts; else echo 'media-e2e skipped (set RUN_E2E=1 to enable)'; fi",
    "test:watch": "vitest --config vitest.browser.config.ts",
    "lint": "eslint tests lib --ext .ts",
    "clean": "rm -rf node_modules/.vite"
  },
  "dependencies": {
    "@moq-web/core": "workspace:*",
    "@moq-web/media": "workspace:*",
    "@moq-web/session": "workspace:*"
  },
  "devDependencies": {
    "@vitest/browser": "4.0.18",
    "@vitest/browser-playwright": "4.0.18",
    "playwright": "^1.40.0",
    "vitest": "4.0.18"
  }
}
